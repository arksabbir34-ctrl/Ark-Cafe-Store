## Packages
framer-motion | Smooth page transitions and scroll-reveals for a premium feel
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms
clsx | Utility for conditional class merging
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
Tailwind Config - extend colors:
Ensure to use CSS variables for colors (already configured in the template, we'll overwrite them in index.css).
Images: using high-quality Unsplash placeholders.
